<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.junya.batterywidget">

    <application android:allowBackup="true"
                 android:label="@string/app_name"
                 android:icon="@mipmap/ic_launcher"
                 android:theme="@style/AppTheme">

        <receiver android:name="BatteryWidget" android:label="BatteryWidget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_settings"/>
        </receiver>
        <service android:name=".BatteryWidget$WidgetService"></service>

    </application>

</manifest>
